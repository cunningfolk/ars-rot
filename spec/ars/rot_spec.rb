require "spec_helper"

RSpec.describe Ars::Rot do
  it "has a version number" do
    expect(Ars::Rot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
